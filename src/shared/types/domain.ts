// Tipos de dominio transversales (compartidos entre módulos).
// Los modelos específicos de cada módulo y sus mappers residen en modules/<modulo>/domain/*.model.ts.

export type ReporteRow = Record<string, unknown>;

